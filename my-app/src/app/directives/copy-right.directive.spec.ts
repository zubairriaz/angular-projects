import { CopyRightDirective } from './copy-right.directive';

describe('CopyRightDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyRightDirective();
    expect(directive).toBeTruthy();
  });
});
